sudo: required
services:
  - docker
before_install:
  - sudo apt-get install jq
  - chmod u+x build.sh
env:
  - ADDON="hassio-wb-mqtt"
script:
  - docker login -u $DOCKER_USER -p $DOCKER_PASS
  - ./build.sh $ADDON